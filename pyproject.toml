[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "rp2350-hid-bridge"
version = "0.2.0"
description = "Python SDK for the RP2350 USB HID bridge"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["rp2350_hid_bridge*"]
